#server:
# tomcat:
#  max-connections: 400
#  max-threads: 400

banking-service:
  ribbon:
    eureka:
      enabled: false
    listOfServers: localhost:8092,localhost:8092
    ServerListRefreshInterval: 15000


logging:
 level:
  root: error
 file: target/app.log
 
